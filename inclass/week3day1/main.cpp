#include <iostream>

using namespace std;

int main()
{   // if it's int or char can use switch if , if else, chain if else, switch
    switch (letter)
{
    case 'a':
    case 'A':
        break;
    case b:
        break;
    case c:
        break;
    case d:
        break;
    default: cout << "bad input" << "\n";
        break;
}


    lessser = (num1 < num2)?num1:num2; /* (boolean)?expr1:expr2 if true exp1, if false expr2 */
    (num1 < num2)?lesser - num1:lesser = num2;

     int change, quarters;

     cout << " Enter number between 1 and 99" << endl;
     cin  >> change;

     quarters = change / 25;

     cout << "You have " << quarters;
     if (( quarters == 1)?" quarter":" quarters") << "\n";


    cout << "Hello world!" << endl;
    return 0;
}
